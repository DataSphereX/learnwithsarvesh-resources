[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "resume-analyzer-starter"
version = "0.1.0"
description = "Starter kit for AI resume analyzer"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
