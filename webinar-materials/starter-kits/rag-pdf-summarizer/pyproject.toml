[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rag-pdf-summarizer-starter"
version = "0.1.0"
description = "Starter kit for PDF summarizer RAG project"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
